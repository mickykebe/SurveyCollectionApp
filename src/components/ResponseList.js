import React from 'react';
import { withStyles } from 'material-ui/styles';

const styles = theme => ({

});

function SurveyResponse({ prevId, curId, nextId}) {
  return (
    
  )
}

export default withStyles(ResponseList);