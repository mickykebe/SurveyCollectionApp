import React from 'react';
import Input from 'material-ui/Input';

function ShortAnswer(props) {
  return (
    <Input placeholder='Short Answer' disabled={true} fullWidth={true} />
  );
}

export default ShortAnswer;